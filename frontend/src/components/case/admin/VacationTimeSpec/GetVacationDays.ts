import { backendUrl } from "../../../assets/strings/backendUrl";

export default async function getVacationDays(
  setMaxDays
) {
  // const headers = {
  //   "Content-Type": "application/json",
  // };

  // const fetchResponse = await fetch(`${backendUrl}/user`, {
  //   method: "GET",
  //   headers: headers,
  //   body: JSON.stringify(userData),
  // });

  // if (!fetchResponse.ok) {
  //   console.log("Failed to get max vacation day");
  // Create alert
  // } else {
  //   const response = await fetchResponse.json();
  //   setMaxDays(response.data)
  // }
}
