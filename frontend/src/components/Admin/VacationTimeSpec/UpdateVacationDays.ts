import { backendUrl } from "../../../assets/strings/backendUrl";

export default async function updateVacationDays(
  maxDays: string
) {
  // const headers = {
  //   "Content-Type": "application/json",
  // };

  // const fetchResponse = await fetch(`${backendUrl}/`, {
  //   method: "PATCH",
  //   headers: headers,
  //   body: JSON.stringify(maxDays),
  // });

  // if (!fetchResponse.ok) {
  //   console.log("Failed to update max vacation days");
  // Create alert
  // } else {
  //   const response = await fetchResponse.json();
  //   Toast Max days was updated
  // }
}
